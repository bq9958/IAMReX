# IAMReX vs. paper — deviations, suspected bugs, decisions

Status legend: **BUG** (fix in IAMReX) · **VARIANT** (defensible alternative; ls2d default = paper, switch reproduces IAMReX) · **DOC** (IAMReX docs wrong, code right) · **OPEN** (needs a numerical test or a reference to decide).

| ID | Where (IAMReX) | Paper says | IAMReX does | Status / decision |
|---|---|---|---|---|
| D1 | `rk_first_reinit`, `rk_second_reinit`, the `else` branch of `ddx/ddy` | S99 (62): if w^L<0 and w^R>0 (expansion / rarefaction) take D = 0 | takes (D^L + D^R)/2 | **VARIANT**. Both give |∇d|→1 at steady state; the average is smoother near kinks (e.g. Zalesak corners). ls2d: `reinit.upwind = godunov|average`, default `godunov`. Test: V1/V2 with both. |
| D2 | `rk_*_reinit` update kernel (`sgntmp`) | S99 (56)-(57): L(d) = S_Δx(φ⁰)(1−|∇d|), S frozen from d⁰ | upwind choice uses frozen `sgn0`, but the coefficient uses S = d/√(d² + (|∇d|·2Δτ)²) recomputed every stage (Peng et al. 1999 form) | **VARIANT**. ls2d: `reinit.sign = frozen|peng`, default `frozen`. |
| D3 | `rk_second_reinit`: `phi_new = phi1 − ½Δτ·s·((|∇phi1|−1) − (|∇phi0|−1))` | S99 (55) as printed: d^{k+1} = d^{(1)} + ½Δτ(L^k + L^{(1)}) — not a consistent RK2 | algebraically equals Heun: d^{k+1} = d^k + ½Δτ(L^k + L^{(1)}) | **DOC**. IAMReX correct; treat (55) as a typo. ls2d implements Heun explicitly. |
| D4 (fixed: IAMReX PR #77, branch `fix/mass-fix-delta-support`) | `mass_fix`, first kernel (`deltafab`) | S99 (68)-(71): H'_ε evaluated at d⁰ everywhere | branch test `if (phi > eps) ... else if (phi > -eps)` uses **current** φ, the value inside uses `phi_ori`. Cells where |φ_cur|≤ε but |φ_ori|>ε get δ = ½(1+cos(πφ_ori/ε))/ε evaluated outside its support (nonzero garbage); cells with |φ_cur|>ε≥|φ_ori| get δ=0 instead of the correct value | **BUG** (small, only band-edge cells). Fix: test on `phiorifab`. Add IAMReX regression: RSV volume drift before/after. |
| D5 (RESOLVED) | `mass_fix`, 9-point (27-point) "integration": centre weight 16 (52), neighbours 1 | Sussman–Fatemi 1999 eq. (4.8): ∫_Ω_ij g ≈ h²/24 (16 g_ij + Σ_{8 neighbours} g) | identical: centre 16, neighbours 1 (sum 24). IAMReX and ls2d implement the paper's rule exactly | none — the earlier sensitivity test (uniform weights worse) is consistent with this |
| D6 | `rk_*_reinit` last kernel: if φ changes sign inside the band → `phi_new = 0.1·phi_old`; outside the band → `Abort` | not in S99 | engineering guard | **VARIANT** (keep, count triggers, print). Never triggers in V1–V3 if Δτ = Δx/2 and ENO is correct — a nonzero count is itself a diagnostic. |
| D7 (docs fixed in PR #77) | `NavierStokes.cpp` ~L2270: `viscn_cc` and `viscnp1_cc` both ← μ(φ^{n+½}) | S99 (16): ρ, μ from φ^{n+½} in both L* and Lⁿ | same as paper | **DOC**: `Docs/.../LevelSet.rst` eq. (viscsolve) writes μ(φⁿ) and μ(φ^{n+1}); fix the docs. |
| D8 | `calculate_eps`: single ε = `ns.epsilon`·Δx_min (default 2) used for H_ε, S, δ | S99: H_ε with ε = αΔx (α=2–3 in §5); sign function S_Δx uses ε = Δx (57) | one ε for everything | **VARIANT** (minor). ls2d: `ls.eps_heaviside`, `ls.eps_sign` separately, defaults 2Δx and 2Δx (=IAMReX); paper mode sets eps_sign = Δx. |
| D9 | LS branch has no surface-tension term | S99 (16),(34)-(39): M = κ∇H/We with nodal normals | absent (RT/breaking-wave inputs don't need it) | v2 of ls2d adds M as in §3.3; not an IAMReX bug. |
| D10 | `NS_LS.cpp::levelset_diffcomp` is an empty body; `reinitialization_consls` calls it | — | conservative-LS reinit path (`reinit_levelset=2,3`) is a stub | out of scope; note in IAMReX docs that only `reinit_levelset=1` is implemented. |
| D11 | Order of steps in `advance_semistaggered_twophase_ls`: reinit runs right after φ advection, before viscous solve / projection; ρ^{n+1}, ρ^{n+½} built from reinitialised φ | S99 §3.1 lists redistance as step 4 (after projection) | different order | **VARIANT**. Both consistent; ls2d default = IAMReX order, switch `reinit_after_projection=1` for paper order. Test on V6. |
| D12 | `mass_fix` denominator guard `phi3 + 1e-12` | — | avoids 0/0 far from the band (δ=0 there so λ irrelevant) | fine; ls2d skips cells with zero denominator explicitly. |

## Things to verify numerically before calling them "confirmed"
1. D4: run RSV (IAMReX `Tutorials/RSV`, 64², T=4) with and without the fix; compare Σ H_ε(φ) drift and the E(80) error at t=T.
2. D1/D2: run V1 (static disk) with the four combinations; report max||∇φ|−1| in the band and zero-contour displacement.
3. D11: run V6 (RT) both orders; expect visually identical, small quantitative difference.
| D13 (fixed in PR #77) | `prob/prob_init.cpp`, comment above `init_TaylorGreen` | For u = sin(2πx)cos(2πy), v = −cos(2πx)sin(2πy) the Bernoulli balance −p_x = u u_x + v u_y = π sin(4πx) gives p = **+**¼(cos4πx + cos4πy)e^{−16π²νt} | comment states p = −¼(…) | **DOC** (comment only; the code does not use it). Verified numerically with ls2d: computed p correlates −0.99999 with the commented formula and +1 with the corrected one. |
| D14 (addressed: IAMReX PR #78) | `Tutorials/RSV` | — | ships with a +17.9 % area gain over one T=4 cycle (64²) that was never compared with the literature | **DOC/TEST**: add the area diagnostic and the reference (Enright 2002 / Rider–Kothe 1998) to the tutorial; consider `alpha_delta`-style narrower H'_eps in `mass_fix` (see validation.md). |
